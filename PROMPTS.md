# Development Prompts

This file contains the original user prompts that guided the development of `latex2img-cli`.

1. **Initial Request**: I want a quick library to convert some text containing latex equations into an image as if it was rendered on a browser and then a screenshot of that rendered content was taken.
2. **Issue Report**: The cli tool does not work. Check what's wrong.
3. **Clarification**: I meant it does not render the input properly.
4. **Testing**: Can you run this one: `./bin.js 'Theorem: \( a^2 + b^2 = c^2 \) and the integral: $$\int x dx$$' -o theorem.png -w 500 -s 30`
5. **Packaging**: Gorgeous. This works. Now package this into a github repo. My github email is prajdabre@gmail.com.
6. **New Requirements**: One, use the brave browser from now on. Two, add examples for cli with images and upload the images.
7. **Scaling Issue**: The matrix algebra one looks cut off. You may want an auto resizer functionality in place.
8. **Stress Testing**: Stress test on other examples too.
9. **Finalizing Examples**: Can you commit, ensuring that the images for the use cases for which the images were generated?
10. **Data Management**: Can you also add the text inputs to generate these images to the repo and commit?
11. **Feature Request**: Can we pimp this up to make it look like handwriting?
12. **Final Request**: Can you dump all the prompts I wrote to code this up into a file and add it to the repo?
